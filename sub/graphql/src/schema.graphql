#
# Copyright 2016 Alien Laboratories, Inc.
#
# https://facebook.github.io/graphql/#sec-Comments
#
# NOTE: PyCharm GraphQL plug-in will attempt to validate against the top-level graphql.config.json file.
#

type User {
  id: ID!
  name: String
}

type Query {
  user(id: ID): User
}

schema {
  query: Query
}
